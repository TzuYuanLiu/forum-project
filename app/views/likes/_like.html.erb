<% like = @post.find_my_like(current_user) %>

This post has
<%= @post.user_post_likes.count %>
likes


 <% if like %>
   <%= link_to "Like", post_like_path(@post, like), :method => :delete, :class => "btn btn-success 	glyphicon glyphicon-thumbs-up", :remote => true  %>
 <% else %>
   <%= link_to "Like", post_likes_path(@post), :method => :post, :class => "btn btn-default	glyphicon glyphicon-thumbs-up", :remote => true %>
 <% end %>